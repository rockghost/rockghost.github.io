Run the sidebar menu removal script on all HTML files in src/:

```
python3 scripts/remove-sidebar-menu.py
```

This removes the left sidebar navigation (`<nav id=lp-left-nav>` ... `</nav>`) from each HTML file in src/, excluding index.html.

Report the results to the user.
